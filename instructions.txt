jenkins-controller
ubuntu 24.04
same key
t3.large
22 8080 80 open bind to your ip
16 gb volume storage. 


https://www.jenkins.io/doc/book/installing/linux/


sudo apt update
sudo apt install fontconfig openjdk-21-jre
java -version

lts-release

sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc \
  https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]" \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update
sudo apt install jenkins


Task 1 deploy:

simple:

1) github repo - app.py, nginx.conf, requirements.txt, Jenkinsfile, Dockerfiles.

2) install docker manually on Jenkins controller - 2.1) curl get.docker.com.... 
                             2.2) add jenkins and ubuntu to the docker group
                             2.3) sudo reboot.

                            
3) Set-up pipeline job on jenkins

4) Jenkinsfile:
        - Clean-up (delete containers)
        - set-up (network)
        - build images

        - Run containers

Manual check - either curl or in browser. 


- trivy - i will send guide. 1) very first stage = file system scan.
                             2) after image has been built: security-scan image - archive results as an archive.
                             3) Gate the image scan - research input{}. 
                             4) manuall install

- unit/int test - 1 unit test - decide where/when. decide post-actions. 
    Error handling. - research catchErrors() - mark failures as unstable. stage/build = unstable. 
    manually instally venv. 
    think about + design solution for requirements that are only required by the pipeline.  

    sh """
      python3 -m venv .venv
      . .venv/bin/activate
      pip install -r requirements.txt
      python3 -m unitest test.py
      deactivate
    """



later:
- switch to my prod dockerfile and get the build working. https://github.dev/chris-reeves1/didactic-rotary-phone
- gating for image size + optional docker slim build. https://github.com/slimtoolkit/slim- optional: use a var/library to give repeatability here.
- parametrization.
- credentials - private repo github token - dockerhub - token.  
- tag + push to dockerhub 
- collect and store metadata. 
- sonarqube setup.- code analysis. 

stretch: professionalise :
- webhook
- paralellism
- scripts
- SBOM

- matrix
- DORA
- Options
